# Regobs / Varsom POC

Run:

```bash
python3 poc.py
```

Outputs a read-only `Search/AtAGlance` sample for snow observations around Besseggen/Jotunheimen. Keep page sizes small; the same radius with 10 records timed out during evaluation.
