# Provenance License Registry

Static POC for source/license registry records and the minimum provenance schema each normalized source should emit.

Outputs:

- `raw/primary-source-notes.json`
- `normalized/license-registry.sample.json`
- `normalized/provenance-record.schema.sample.json`

Verdict: `viable`. This should become a shared convention before expanding the data-source evaluation.
