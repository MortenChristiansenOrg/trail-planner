# Curated Seed List

Manual seed-route POC for candidate discovery. The sample stores minimal facts, source URLs, route-type hints, manual quality tags, and known logistics gaps.

Outputs:

- `raw/manual-source-log.json`
- `normalized/seed-routes.sample.json`

Verdict: `viable`. This is MVP-critical because automated discovery will need editorial seeds and source-backed route IDs.
